name: mostbyte_print
description: Package for printing recipt by ethernet interface without driver
version: 0.0.1
homepage: https://github.com/azizbekvahidov/print_reciept.git

environment:
  sdk: '>=3.1.3 <4.0.0'
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  pos_printer_manager: ^0.0.5
  webcontent_converter: ^0.0.8+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
 